spring:
  cloud:
    config:
      name: microservicecloud-config-client #需要从git远程库上读取的资源名称，注意没有yml后缀名
      profile: dev #本次访问的配置项
      label: master
      uri: http://config-3344.com:3344 #本微服务启动后先去找3344号服务，通过SpringCloudConfig获取Git远程库的服务地址